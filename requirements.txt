# Install a PyTorch build appropriate for your CUDA runtime first.
torch>=2.5,<3
numpy>=1.24,<3
scipy>=1.11,<2
h5py>=3.10,<4
PyYAML>=6,<7
nibabel>=5.2,<6
tqdm>=4.66,<5
# Qwen3-VL support is required; smoke tests do not load this model.
transformers>=4.57,<6
safetensors>=0.4
# Optional: flash-attn (SDPA is the default), wandb (disabled by default),
# pytorch-fid (only for optional slice-FID with local, user-supplied weights).
